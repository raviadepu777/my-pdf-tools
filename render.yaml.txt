services:
  # This is a Web Service
  - type: web
    name: 1pdfword-app
    # Use the Native Node Environment
    env: node
    # Choose a plan (can be free or starter)
    plan: free
    # Specify the system packages to install
    systemPackages:
      - name: libreoffice
    # Define the build and start commands
    buildCommand: "npm install"
    startCommand: "node server.js"